---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: cloudflareproviders.swacd.swacd.io
spec:
  group: swacd.swacd.io
  names:
    kind: CloudflareProvider
    listKind: CloudflareProviderList
    plural: cloudflareproviders
    singular: cloudflareprovider
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareProviderSpec defines the desired state of CloudflareProvider
            properties:
              accountID:
                type: string
              apiTokenSecretRef:
                description: SecretRef defines a reference to a Kubernetes Secret
                  key.
                properties:
                  key:
                    description: Key inside the Secret data
                    type: string
                  name:
                    description: Name of the Secret
                    type: string
                required:
                - key
                - name
                type: object
              zoneName:
                type: string
            required:
            - accountID
            - apiTokenSecretRef
            - zoneName
            type: object
          status:
            description: CloudflareProviderStatus defines the observed state of CloudflareProvider
            properties:
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
