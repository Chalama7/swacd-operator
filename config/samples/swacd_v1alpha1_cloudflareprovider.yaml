apiVersion: swacd.swacd.io/v1alpha1
kind: CloudflareProvider
metadata:
  name: cloudflare-prod
spec:
  zoneName: edge.cloudcfg.io
  accountID: "example-account-id"          # 🔹 Replace with your actual Cloudflare account ID
  apiTokenSecretRef:
    name: cloudflare-api-token             # 🔹 Secret name in same namespace
    key: apiToken                         # 🔹 Required key field inside the Secret